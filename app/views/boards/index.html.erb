<h1>Boards</h1>
<%= link_to 'New Board', new_board_path, class: "ui blue button inverted" %>

  <% @boards.each do |board| %>
    <%= link_to board.name, board_path(board), class:"board"%>
  <% end %>        
  </div>
</div>